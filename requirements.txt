# MediCare Connect — Python Dependencies
# Phase 1: Database Foundation

# Postgres driver
psycopg2-binary==2.9.9

# Environment variable management
python-dotenv==1.0.1

# HTTP client (used from Phase 3 onward for calendar APIs)
httpx==0.27.0

# UUID handling
# (built into Python stdlib — no extra package needed)
